export enum Formats {
    traslitteration = 'traslitteration',
    empty_character_number = 'empty_character_number',
    previous_sentence = 'previous_sentence',
    next_sentence = 'next_sentence',
}

export enum TraslitterationFormat {
    STANDARD = 'standard',
    UPPERCASE = 'uppercase',
    LOWERCASE = 'lowercase',
    SENTENCE_CASE = 'sentence_case',
    TITLE_CASE = 'title_case',
}
